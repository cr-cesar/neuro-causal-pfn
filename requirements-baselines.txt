# Optional stacks, installed on top of requirements.txt when the step that
# needs them arrives. Nothing here is imported by the Phase-1 encoder training.
#
# 1) Tier-4 production evaluator (off-the-shelf CausalPFN over frozen latents)
#    and the TabICL reference implementation. causalpfn requires faiss>=1.9,
#    whose PyPI wheels need a recent glibc: on Myriad's older system image pip
#    tries to compile faiss from source and fails. There, install faiss through
#    conda first (conda install -c conda-forge "faiss-cpu>=1.9.0") or run the
#    Tier-4 production scoring on a machine with a newer glibc.
causalpfn==0.1.4
tabicl==0.1.4
faiss-cpu>=1.9.0
huggingface_hub==0.23.*

# 2) Imaging conveniences (augmentation/IO helpers beyond nibabel)
monai==1.3.*
torchio==0.19.*
nilearn==0.10.*

# 3) Experiment-tracking and config tooling
wandb==0.17.*
hydra-core==1.3.2
omegaconf==2.3.*
pytorch-lightning==2.2.*
einops==0.8.0
pandas==2.2.*
