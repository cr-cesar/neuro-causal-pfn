# Core environment: everything Phase 1 actually imports (the VAE arms, the
# experiments runner with its default T-learner Tier-4 proxy, and the tests).
#
#   module load python3/3.11
#   python3 -m venv ~/venvs/neuro && source ~/venvs/neuro/bin/activate
#   pip install --upgrade pip
#   pip install -r requirements.txt
#
# The Tier-4 evaluator stack (causalpfn + faiss) is NOT needed to train the
# encoders; install it later from requirements-baselines.txt (see the notes
# there — on old-glibc clusters like Myriad, faiss needs conda).
#
# The extra index serves the CUDA 11.8 build of torch, which targets the V100
# nodes (sm_70); PyPI's default CUDA build also works with a recent driver.
--extra-index-url https://download.pytorch.org/whl/cu118

torch==2.3.1
numpy==1.26.*          # torch 2.3 is built against numpy 1.x
scipy==1.13.*
scikit-learn==1.4.*
nibabel==5.2.*
pytest>=8.0

# the project itself, in editable mode
-e .
